import { ContentTypeEqualsPipe } from './content-type-equals.pipe';

describe('ContentTypeEqualsPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentTypeEqualsPipe();
    expect(pipe).toBeTruthy();
  });
});
